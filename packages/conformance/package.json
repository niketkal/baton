{
  "name": "@baton/conformance",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "description": "Baton public conformance suite (synthetic fixtures only).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "cases", "README.md"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@baton/lint": "workspace:*",
    "@baton/render": "workspace:*",
    "@baton/schema": "workspace:*"
  }
}
